<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <table border="2">
        <tr>
            <th>ID</th>
            <th>Name</th>
        </tr>
        
        <tr {%for c in classroom %}>
            <td>{{c.id}}</td>
            <td>{{c.name}}</td>
            <td><a href="{{path('delete',{'id':c.id})}}">
                Supprimer</a></td>
        </tr {% endfor %}> 
    </table>
</body>
</html>